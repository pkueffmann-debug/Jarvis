// Phase 5: Google Calendar API

async function getTodayEvents() {
  throw new Error('Calendar not yet configured — coming in Phase 5');
}

async function createEvent(_title, _startTime, _endTime, _attendees = []) {
  throw new Error('Calendar not yet configured — coming in Phase 5');
}

module.exports = { getTodayEvents, createEvent };
